<div class="container">
<div class="row">
  <div class="span7">
    <h2>
      <a href="<%=url_for @workshop%>"><%=@workshop.name%></a>
    </h2>
    <div class="text"><%= markdown(@workshop.description)%></div>
    <div class="text"><%= markdown(@workshop.details)%></div>
    <hr/>
  </div>
  <div class="span5">
    <h2>Calendar</h2>
    <%= markdown(@workshop.calendar || '')%>
    <%=render 'sharing'%>    
  </div>
</div>
</div>

<%content_for :title do%><%=@workshop.name%> : <%end%>